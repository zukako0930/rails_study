<table>
    <tr>
        <th>ISBNコード</th><th>書籍</th>
    </tr>
<% @books.each do |book| %>
    <tr>
        <td><%= book.isbn %></td>
        <td><%= book.title %></td>
    </tr>
<% end %>
</table>
<!--<%= debug(@books) %>-->
<br />
<% @current = capture do%>
今の時刻は<%= Time.now %>だよ。<br>
<%= format_datetime(Time.now, :datetime) %>
<% end %>
<%= @current %><br />
<div style="color:Red"><%= @current %></div>

