<%= form_with(model: fan_comment, local: true) do |form| %>
  <% if fan_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fan_comment.errors.count, "error") %> prohibited this fan_comment from being saved:</h2>

      <ul>
      <% fan_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :author_no %>
    <%= form.number_field :author_no, id: :fan_comment_author_no %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :fan_comment_name %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :fan_comment_body %>
  </div>

  <div class="field">
    <%= form.label :deleted %>
    <%= form.check_box :deleted, id: :fan_comment_deleted %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
